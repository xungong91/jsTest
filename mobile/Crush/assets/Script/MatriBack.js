cc.Class({
    extends: cc.Component,

    properties: {
        itemSize : {
            default : null,
            type : cc.size
        }
    },

    // use this for initialization
    onLoad: function () {
        // var sprite = cc.Sprite("Texture/PatternBg.png");
        // this.node.addChild(sprite);
    },

    // called every frame, uncomment this function to activate update callback
    // update: function (dt) {

    // },
});
